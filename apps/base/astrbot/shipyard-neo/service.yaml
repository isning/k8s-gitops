apiVersion: v1
kind: Service
metadata:
  name: bay
  labels:
    app.kubernetes.io/name: bay
    app.kubernetes.io/part-of: shipyard-neo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bay
  ports:
    - name: http
      port: 8114
      targetPort: 8114
      protocol: TCP
