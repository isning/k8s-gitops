apiVersion: v1
kind: Service
metadata:
  name: speaches
  labels:
    app: speaches
spec:
  type: ClusterIP
  selector:
    app: speaches
  ports:
    - name: http
      port: 8000
      targetPort: http
