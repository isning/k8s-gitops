# Flux HelmRepository — redroid-operator Helm chart repository.
# Served from the gh-pages branch of the GitHub repository.
# See: https://isning.github.io/redroid-operator
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: redroid-operator
  namespace: flux-system
spec:
  type: oci
  interval: 12h
  url: oci://ghcr.io/isning/charts
