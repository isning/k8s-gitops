apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: astrbot
  labels:
    app: astrbot
spec:
  selector:
    matchLabels:
      app: astrbot
  endpoints:
    - port: http-webui
      path: /metrics
      interval: 20s