---
apiVersion: v1
kind: Service
metadata:
  name: proxy-svc
  namespace: egress-system
spec:
  ports:
  - port: 7890
    name: http-proxy
    appProtocol: http
    protocol: TCP
    targetPort: 7890
  selector:
    app: proxy-engine
